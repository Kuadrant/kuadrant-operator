apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  creationTimestamp: null
  labels:
    app: kuadrant
  name: kuadrantcontrolplanes.kuadrant.io
spec:
  group: kuadrant.io
  names:
    kind: KuadrantControlPlane
    listKind: KuadrantControlPlaneList
    plural: kuadrantcontrolplanes
    shortNames:
    - kcp
    singular: kuadrantcontrolplane
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].reason
      name: Reason
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          KuadrantControlPlane manages the lifecycle of Kuadrant component operators.
          The operator auto-creates a singleton named "default" on startup.
          Deleting this resource is a no-op — the operator re-creates it immediately.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          status:
            description: KuadrantControlPlaneStatus defines the observed state of
              the control plane.
            properties:
              components:
                description: Components reports the status of each managed component
                  operator.
                items:
                  description: ComponentStatus reports the observed state of a single
                    component operator.
                  properties:
                    chartVersion:
                      description: ChartVersion is the version from the synced Helm
                        chart's Chart.yaml.
                      type: string
                    crds:
                      description: CRDs reports establishment status of CRDs managed
                        by this component.
                      items:
                        description: CRDStatus reports the observed state of a single
                          CRD.
                        properties:
                          established:
                            description: Established indicates whether the CRD is
                              accepted by the API server.
                            type: boolean
                          name:
                            description: Name of the CRD (e.g., "dnsrecords.kuadrant.io").
                            type: string
                        required:
                        - established
                        - name
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - name
                      x-kubernetes-list-type: map
                    images:
                      description: Images reports the RELATED_IMAGE env var values
                        for this component.
                      items:
                        description: ImageStatus reports a single image reference
                          from a RELATED_IMAGE env var.
                        properties:
                          image:
                            description: Image is the RELATED_IMAGE env var value.
                            type: string
                          name:
                            description: Name identifies this image (e.g., "controller",
                              "broker").
                            type: string
                        required:
                        - image
                        - name
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - name
                      x-kubernetes-list-type: map
                    name:
                      description: Name of the component (e.g., "dns-operator").
                      type: string
                    ready:
                      description: Ready indicates whether the component Deployment
                        has available replicas.
                      type: boolean
                  required:
                  - name
                  - ready
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              conditions:
                description: Conditions represent the latest available observations.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed.
                format: int64
                type: integer
            type: object
        type: object
        x-kubernetes-validations:
        - message: KuadrantControlPlane name must be default
          rule: self.metadata.name == 'default'
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
