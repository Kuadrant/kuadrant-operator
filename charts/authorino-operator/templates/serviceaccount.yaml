{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "authorino-operator.serviceAccountName" . }}
  labels:
    {{- include "authorino-operator.labels" . | nindent 4 }}
{{- end }}
