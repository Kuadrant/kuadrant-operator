apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "limitador-operator.fullname" . }}-leader-election
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "limitador-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "limitador-operator.fullname" . }}-leader-election
subjects:
- kind: ServiceAccount
  name: {{ include "limitador-operator.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
