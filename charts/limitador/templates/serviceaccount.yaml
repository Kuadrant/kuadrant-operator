{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "limitador.serviceAccountName" . }}
  labels:
    {{- include "limitador.labels" . | nindent 4 }}
{{- end }}
