apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "developer-portal-controller.fullname" . }}-manager-rolebinding
  labels:
    {{- include "developer-portal-controller.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "developer-portal-controller.fullname" . }}-manager-role
subjects:
  - kind: ServiceAccount
    name: {{ include "developer-portal-controller.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
