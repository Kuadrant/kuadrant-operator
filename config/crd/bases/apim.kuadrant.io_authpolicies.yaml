---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: authpolicies.apim.kuadrant.io
spec:
  group: apim.kuadrant.io
  names:
    kind: AuthPolicy
    listKind: AuthPolicyList
    plural: authpolicies
    singular: authpolicy
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            properties:
              policy:
                description: Policy per action type but also per provider if using
                  custom type.
                items:
                  properties:
                    action:
                      description: The action to take if the request is matches with
                        the rules.
                      enum:
                      - ALLOW
                      - CUSTOM
                      - DENY
                      - AUDIT
                      type: string
                    provider:
                      default: ""
                      description: Specifies detailed configuration of the CUSTOM
                        action. Must be used only with CUSTOM action.
                      type: string
                    rules:
                      description: A list of rules to match the request. A match occurs
                        when at least one rule matches the request.
                      items:
                        description: 'TODO(rahul): remove the following wrappers once
                          https://github.com/istio/api/issues/2352 is fixed.'
                        properties:
                          from:
                            items:
                              properties:
                                hosts:
                                  items:
                                    type: string
                                  type: array
                                methods:
                                  items:
                                    type: string
                                  type: array
                                not_hosts:
                                  items:
                                    type: string
                                  type: array
                                not_methods:
                                  items:
                                    type: string
                                  type: array
                                not_paths:
                                  items:
                                    type: string
                                  type: array
                                not_ports:
                                  items:
                                    type: string
                                  type: array
                                paths:
                                  items:
                                    type: string
                                  type: array
                                ports:
                                  items:
                                    type: string
                                  type: array
                              type: object
                            type: array
                        type: object
                      type: array
                  required:
                  - action
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - action
                - provider
                x-kubernetes-list-type: map
              targetRef:
                description: TargetRef identifies an API object to apply policy to.
                properties:
                  group:
                    description: Group is the group of the target resource.
                    maxLength: 253
                    pattern: ^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                    type: string
                  kind:
                    description: Kind is kind of the target resource.
                    maxLength: 63
                    minLength: 1
                    pattern: ^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$
                    type: string
                  name:
                    description: Name is the name of the target resource.
                    maxLength: 253
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace is the namespace of the referent. When
                      unspecified, the local namespace is inferred. Even when policy
                      targets a resource in a different namespace, it MUST only apply
                      to traffic originating from the same namespace as the policy.
                    maxLength: 63
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                required:
                - group
                - kind
                - name
                type: object
            required:
            - targetRef
            type: object
        type: object
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
