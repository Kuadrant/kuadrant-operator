---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: kuadrants.kuadrant.io
spec:
  group: kuadrant.io
  names:
    kind: Kuadrant
    listKind: KuadrantList
    plural: kuadrants
    singular: kuadrant
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[0].reason
      name: Status
      priority: 2
      type: string
    - jsonPath: .status.mtlsAuthorino
      name: mTLS Authorino
      type: boolean
    - jsonPath: .status.mtlsLimitador
      name: mTLS Limitador
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Kuadrant configures installations of Kuadrant Service Protection
          components
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KuadrantSpec defines the desired state of Kuadrant
            properties:
              mtls:
                description: |-
                  MTLS is an optional entry which when enabled is set to true, kuadrant-operator
                  will add the configuration required to enable mTLS between an Istio provided
                  gateway and the Kuadrant components.
                properties:
                  authorino:
                    type: boolean
                  enable:
                    type: boolean
                  limitador:
                    type: boolean
                type: object
              observability:
                properties:
                  enable:
                    type: boolean
                type: object
              resilience:
                description: Resilience is an optional entry which enables different
                  control plane resilience features.
                properties:
                  counterStorage:
                    description: Storage contains the options for Limitador counters
                      database or in-memory data storage
                    properties:
                      disk:
                        properties:
                          optimize:
                            description: DiskOptimizeType defines the valid options
                              for "optimize" option of the disk persistence type
                            enum:
                            - throughput
                            - disk
                            type: string
                          persistentVolumeClaim:
                            properties:
                              resources:
                                description: |-
                                  Resources represents the minimum resources the volume should have.
                                  Ignored when VolumeName field is set
                                properties:
                                  requests:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: |-
                                      Storage Resource requests to be used on the PersistentVolumeClaim.
                                      To learn more about resource requests see:
                                      https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                required:
                                - requests
                                type: object
                              storageClassName:
                                type: string
                              volumeName:
                                description: VolumeName is the binding reference to
                                  the PersistentVolume backing this claim.
                                type: string
                            type: object
                        type: object
                      redis:
                        properties:
                          configSecretRef:
                            description: |-
                              LocalObjectReference contains enough information to let you locate the
                              referenced object inside the same namespace.
                            properties:
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                            type: object
                            x-kubernetes-map-type: atomic
                        type: object
                      redis-cached:
                        properties:
                          configSecretRef:
                            description: |-
                              LocalObjectReference contains enough information to let you locate the
                              referenced object inside the same namespace.
                            properties:
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                            type: object
                            x-kubernetes-map-type: atomic
                          options:
                            properties:
                              batch-size:
                                description: 'BatchSize defines the size of entries
                                  to flush in as single flush [default: 100]'
                                type: integer
                              flush-period:
                                description: 'FlushPeriod for counters in milliseconds
                                  [default: 1000]'
                                type: integer
                              max-cached:
                                description: 'MaxCached refers to the maximum amount
                                  of counters cached [default: 10000]'
                                type: integer
                              response-timeout:
                                description: 'ResponseTimeout defines the timeout
                                  for Redis commands in milliseconds [default: 350]'
                                type: integer
                            type: object
                        type: object
                    type: object
                  rateLimiting:
                    type: boolean
                type: object
                x-kubernetes-validations:
                - message: resilience.counterStorage needs to be explictly configured
                    when using resilience.rateLimiting.
                  rule: 'has(self.rateLimiting) ? (self.rateLimiting == true && has(self.counterStorage))
                    || (self.rateLimiting == false && has(self.counterStorage)) ||
                    (self.rateLimiting == false && !has(self.counterStorage)) : true'
            type: object
          status:
            description: KuadrantStatus defines the observed state of Kuadrant
            properties:
              conditions:
                description: |-
                  Represents the observations of a foo's current state.
                  Known .status.conditions.type are: "Available"
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              mtlsAuthorino:
                description: Mtls Authorino reflects the mtls feature state regarding
                  comms with authorino.
                type: boolean
              mtlsLimitador:
                description: Mtls Limitador reflects the mtls feature state regarding
                  comms with limitador.
                type: boolean
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed spec.
                format: int64
                type: integer
              resilience:
                description: Resilience reflects the resilience deployment state
                properties:
                  counterStorage:
                    type: boolean
                  rateLimiting:
                    type: boolean
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
