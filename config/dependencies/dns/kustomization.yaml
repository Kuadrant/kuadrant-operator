resources:
- github.com/kuadrant/dns-operator/config/default?ref=remove_managed_zone_api

patches:
  - path: deployment_patch.yaml
    target:
      kind: Deployment
      name: dns-operator-controller-manager
