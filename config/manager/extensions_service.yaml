---
apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: controller-manager
  name: extensions
  namespace: system
spec:
  ports:
  - name: extensions
    port: 50052
    protocol: TCP
    targetPort: extensions
  selector:
    control-plane: controller-manager
