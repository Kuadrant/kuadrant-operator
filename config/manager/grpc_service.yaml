---
apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: controller-manager
  name: grpc
  namespace: system
spec:
  ports:
  - name: grpc
    port: 50051
    protocol: TCP
    targetPort: grpc
  selector:
    control-plane: controller-manager
