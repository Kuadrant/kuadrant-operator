---
apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: controller-manager
  name: wasm
  namespace: system
spec:
  ports:
  - name: wasm
    port: 8082
    protocol: TCP
    targetPort: wasm
  selector:
    control-plane: controller-manager
