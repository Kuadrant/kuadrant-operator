kuadrant-operator:
  version: "0.0.0"

olm:
  channels: 
  - "alpha"
  default-channel: "alpha"

dependencies:
  authorino-operator: "0.0.0"
  console-plugin: "0.0.0"
  dns-operator: "0.0.0"
  limitador-operator: "0.0.0"
  wasm-shim: "0.0.0"
